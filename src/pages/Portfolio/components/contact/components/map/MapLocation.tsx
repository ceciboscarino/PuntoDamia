import { MapContainer } from "./styled-components";
import { useMemo, useState } from "react"
import { GoogleMap, useLoadScript, MarkerF, InfoWindow, } from "@react-google-maps/api";



export default function MapLocation (){
        return(
                <MapContainer>
                        
                </MapContainer>
        )
}